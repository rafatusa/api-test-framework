# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: deploy
'on':
  workflow_dispatch: {}
jobs:
  lint:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    - name: Install linters
      run: pip install flake8 black isort
    - name: Format with black
      run: black app/ tests/
    - name: Sort imports with isort
      run: isort app/ tests/
    - name: Run flake8
      run: flake8 app/ tests/ --max-line-length=120 --extend-ignore=E203 --exclude=__pycache__,migrations
  test:
    runs-on: ubuntu-latest
    needs:
    - lint
    timeout-minutes: 15
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    - name: Install dependencies
      run: pip install -r requirements-test.txt
    - name: Run unit tests
      run: pytest tests/unit/ -v --tb=short
  provision:
    runs-on: ubuntu-latest
    needs:
    - test
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1
      TF_VAR_project_name: ${{ secrets.PROJECT_NAME }}
      TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
    timeout-minutes: 30
    outputs:
      instance_ip: ${{ steps.tf_output.outputs.instance_ip }}
    steps:
    - uses: actions/checkout@v4
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_wrapper: false
    - name: Terraform Init
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\n"
      working-directory: infra
    - name: Terraform Apply
      run: terraform apply -input=false -auto-approve
      working-directory: infra
    - id: tf_output
      name: Get Outputs
      run: 'ip=$(terraform output -raw instance_ip)

        echo "instance_ip=$ip" >> "$GITHUB_OUTPUT"

        '
      working-directory: infra
  configure:
    runs-on: ubuntu-latest
    needs:
    - provision
    env:
      JWT_SECRET_KEY: ${{ secrets.JWT_SECRET_KEY }}
    timeout-minutes: 30
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    - name: Install Ansible
      run: pip install ansible-core
    - name: Write SSH key
      run: 'mkdir -p ~/.ssh

        printf ''%s\n'' "$SSH_KEY" > ~/.ssh/deploy_key

        chmod 600 ~/.ssh/deploy_key

        '
      env:
        SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
    - name: Scan host key
      run: 'ssh-keyscan -H "${{ needs.provision.outputs.instance_ip }}" >> ~/.ssh/known_hosts

        '
    - name: Write inventory
      run: "printf '[api]\\n%s ansible_user=%s ansible_ssh_private_key_file=~/.ssh/deploy_key\\\
        n' \\\n  \"${{ needs.provision.outputs.instance_ip }}\" \\\n  \"${{ secrets.SSH_USER\
        \ }}\" > ansible/inventory.ini\n"
    - name: Run Ansible playbook
      run: ansible-playbook -i ansible/inventory.ini ansible/site.yml
      env:
        ANSIBLE_HOST_KEY_CHECKING: 'False'
        JWT_SECRET_KEY: ${{ secrets.JWT_SECRET_KEY }}
  verify:
    runs-on: ubuntu-latest
    needs:
    - configure
    - provision
    timeout-minutes: 10
    steps:
    - uses: actions/checkout@v4
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_wrapper: false
    - id: tf_ip
      name: Re-read instance IP
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\nip=$(terraform\
        \ output -raw instance_ip)\necho \"ip=$ip\" >> \"$GITHUB_OUTPUT\"\n"
      working-directory: infra
      env:
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        AWS_REGION: us-east-1
    - name: Health check with retries
      run: "curl --fail --retry 15 --retry-delay 15 --retry-all-errors \\\n  http://${{\
        \ steps.tf_ip.outputs.ip }}/health\n"
  api_tests:
    runs-on: ubuntu-latest
    needs:
    - verify
    - provision
    timeout-minutes: 30
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_wrapper: false
    - id: tf_ip
      name: Re-read instance IP
      run: "terraform init -input=false -reconfigure \\\n  -backend-config=\"bucket=${{\
        \ secrets.TF_STATE_BUCKET }}\" \\\n  -backend-config=\"key=${{ secrets.PROJECT_NAME\
        \ }}/terraform.tfstate\" \\\n  -backend-config=\"region=us-east-1\"\nip=$(terraform\
        \ output -raw instance_ip)\necho \"ip=$ip\" >> \"$GITHUB_OUTPUT\"\n"
      working-directory: infra
      env:
        AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
        AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        AWS_REGION: us-east-1
    - name: Install test dependencies
      run: pip install -r requirements-test.txt
    - name: Create reports directory
      run: mkdir -p reports
    - name: Run API test suite
      run: "pytest tests/api/ -v \\\n  --base-url=\"http://${{ steps.tf_ip.outputs.ip\
        \ }}\" \\\n  --html=reports/test-report.html \\\n  --self-contained-html \\\
        \n  --md-report \\\n  --md-report-output=reports/test-report.md \\\n  --tb=short\
        \ \\\n  -p no:warnings\n"
      env:
        API_BASE_URL: http://${{ steps.tf_ip.outputs.ip }}
    - name: Generate Postman collection
      run: "python scripts/generate_postman.py \\\n  --base-url \"http://${{ steps.tf_ip.outputs.ip\
        \ }}\" \\\n  --output reports/postman_collection.json\n"
      env:
        API_BASE_URL: http://${{ steps.tf_ip.outputs.ip }}
    - name: Upload test artifacts
      uses: actions/upload-artifact@v4
      with:
        name: test-reports
        path: reports/
        retention-days: 30
